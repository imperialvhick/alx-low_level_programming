#include "main.h"

/**
 * print_square - prints a square
 * @size: size of square
 * return: void
 */

void print_square(int size)
{
	int a, b;

	if (size <= o)
		_putchar('\n');
	for (a = 0; a < size; a++)
	{
		for (b = 0; b < (size); b++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}
}

